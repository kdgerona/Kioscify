export const app = {
  app_name: 'APP_NAME',
  node_env: 'NODE_ENV',
  port: 'PORT',
  base_url: 'BASE_URL',
};

export const database = {
  database_url: 'DATABASE_URL',
};

export const auth = {
  jwt_secret: 'JWT_SECRET',
  jwt_expires_in: 'JWT_EXPIRES_IN',
};
