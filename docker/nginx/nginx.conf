# ================================
# Kioscify — Nginx Configuration
# ================================
#
# Domain structure:
#   kioscify.com           → Store Portal (kioscify-store)
#   store.kioscify.com     → Store Portal (alias)
#   platform.kioscify.com  → Platform Admin (kioscify-platform)
#   *.kioscify.com         → Company + Brand Portal (kioscify-company)
#                                     (middleware validates company slug)
#
# SSL: requires wildcard cert for *.kioscify.com + kioscify.com
# Generate with: certbot certonly --dns-<provider> -d kioscify.com -d *.kioscify.com

user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
    use epoll;
    multi_accept on;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;

    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;

    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml application/json application/javascript
               application/xml application/rss+xml image/svg+xml;

    # Rate limiting zones
    limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;
    limit_req_zone $binary_remote_addr zone=login_limit:10m rate=5r/m;   # strict for auth
    limit_req_zone $binary_remote_addr zone=general_limit:10m rate=30r/s;

    # Upstream servers
    upstream api_backend {
        server api:3000;
        keepalive 32;
    }

    upstream store_frontend {
        server store:3000;        # kioscify-store (was kioskly-admin)
        keepalive 16;
    }

    upstream company_frontend {
        server company:3001;      # kioscify-company
        keepalive 16;
    }

    upstream platform_frontend {
        server platform:3002;     # kioscify-platform
        keepalive 16;
    }

    upstream minio_backend {
        server minio:9000;
        keepalive 16;
    }

    # ─── HTTP → HTTPS redirect (all domains) ─────────────────────────────────
    server {
        listen 80;
        server_name kioscify.com *.kioscify.com;
        return 301 https://$host$request_uri;
    }

    # ─── Platform Admin (platform.kioscify.com) ─────────────────────
    server {
        listen 443 ssl;
        http2 on;
        server_name platform.kioscify.com;

        ssl_certificate     /etc/nginx/ssl/live/kioscify.com/fullchain.pem;
        ssl_certificate_key /etc/nginx/ssl/live/kioscify.com/privkey.pem;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_prefer_server_ciphers on;
        ssl_session_cache shared:SSL:50m;
        ssl_session_timeout 1d;

        add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
        client_max_body_size 10M;

        location /storage/ {
            proxy_pass http://minio_backend/kioscify/;
            proxy_set_header Host minio;
            expires 1d;
            add_header Cache-Control "public, immutable";
            add_header Cross-Origin-Resource-Policy "cross-origin" always;
        }

        location / {
            proxy_pass http://platform_frontend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }

        location /_next/static {
            proxy_pass http://platform_frontend;
            expires 365d;
            add_header Cache-Control "public, immutable";
        }
    }

    # ─── Store Portal (root domain + store.* alias) ───────────────────────────
    server {
        listen 443 ssl;
        http2 on;
        server_name kioscify.com store.kioscify.com;

        ssl_certificate     /etc/nginx/ssl/live/kioscify.com/fullchain.pem;
        ssl_certificate_key /etc/nginx/ssl/live/kioscify.com/privkey.pem;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_prefer_server_ciphers on;
        ssl_session_cache shared:SSL:50m;
        ssl_session_timeout 1d;

        add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
        client_max_body_size 200M;

        # APK uploads — no rate limit, extended timeouts (release APKs can be 100MB+)
        location = /api/v1/app-releases {
            proxy_pass http://api_backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Connection "";
            proxy_read_timeout 300s;
            proxy_send_timeout 300s;
            proxy_connect_timeout 10s;
        }

        # API
        location /api/ {
            limit_req zone=api_limit burst=20 nodelay;
            proxy_pass http://api_backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Connection "";
        }

        # Strict rate limit on auth endpoints
        location ~ ^/api/v1/auth/(login|company-login|platform-login) {
            limit_req zone=login_limit burst=10 nodelay;
            proxy_pass http://api_backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Fallback for old /uploads/* URLs in DB — remove after running migrate:minio
        location /uploads/ {
            proxy_pass http://api_backend;
            proxy_cache_valid 200 1d;
            expires 1d;
            add_header Cache-Control "public, immutable";
            proxy_hide_header Cross-Origin-Resource-Policy;
            add_header Cross-Origin-Resource-Policy "cross-origin" always;
        }

        # Object storage — MinIO files served cross-origin for company/brand portals
        location /storage/ {
            proxy_pass http://minio_backend/kioscify/;
            proxy_set_header Host minio;
            expires 1d;
            add_header Cache-Control "public, immutable";
            add_header Cross-Origin-Resource-Policy "cross-origin" always;
        }

        # Health check (no auth, no rate limit)
        location = /health {
            proxy_pass http://api_backend;
            access_log off;
        }

        # Store Portal frontend
        location / {
            proxy_pass http://store_frontend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }

        location /_next/static {
            proxy_pass http://store_frontend;
            expires 365d;
            add_header Cache-Control "public, immutable";
        }
    }

    # ─── Upload-only endpoint (DNS-only, bypasses Cloudflare size limits) ────────
    # Set uploads.kioscify.com to "DNS only" (gray cloud) in Cloudflare so this
    # server block is reached directly — no Cloudflare upload size cap.
    server {
        listen 443 ssl;
        http2 on;
        server_name uploads.kioscify.com;

        ssl_certificate     /etc/nginx/ssl/live/kioscify.com/fullchain.pem;
        ssl_certificate_key /etc/nginx/ssl/live/kioscify.com/privkey.pem;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_prefer_server_ciphers on;
        ssl_session_cache shared:SSL:50m;
        ssl_session_timeout 1d;

        add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
        client_max_body_size 500M;

        location / {
            proxy_pass http://api_backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Connection "";
            proxy_request_buffering off;
            proxy_read_timeout 600s;
            proxy_send_timeout 600s;
            proxy_connect_timeout 10s;
        }
    }

    # ─── Company + Brand Portal (wildcard subdomains) ─────────────────────────
    # IMPORTANT: This catches ALL other subdomains (e.g., greatserve.kioscify.com).
    # The Next.js middleware in kioscify-company validates the subdomain against the DB.
    server {
        listen 443 ssl;
        http2 on;
        server_name ~^(?<subdomain>.+)\.kioscify\.com$;

        ssl_certificate     /etc/nginx/ssl/live/kioscify.com/fullchain.pem;
        ssl_certificate_key /etc/nginx/ssl/live/kioscify.com/privkey.pem;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_prefer_server_ciphers on;
        ssl_session_cache shared:SSL:50m;
        ssl_session_timeout 1d;

        add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
        client_max_body_size 50M;

        location /storage/ {
            proxy_pass http://minio_backend/kioscify/;
            proxy_set_header Host minio;
            expires 1d;
            add_header Cache-Control "public, immutable";
            add_header Cross-Origin-Resource-Policy "cross-origin" always;
        }

        location / {
            proxy_pass http://company_frontend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }

        location /_next/static {
            proxy_pass http://company_frontend;
            proxy_set_header Host $host;
            expires 365d;
            add_header Cache-Control "public, immutable";
        }

        location = /nginx_status {
            stub_status on;
            allow 127.0.0.1;
            allow 172.16.0.0/12;
            deny all;
        }
    }
}
